---
tls_skip_verify: true
#vault_keys: "{{ lookup('file', '{{ vault_unseal_keys_file }}') }}" #if already initialised root token may exist in file so read it in so that audit check can be done
vault_keys:
  slurp:
    src: "{{ vault_unseal_keys_file }}"